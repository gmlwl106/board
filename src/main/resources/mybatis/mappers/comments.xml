<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="comments">

	<!-- 댓글 리스트 가져오기 -->
	<select id="getCmtList" parameterType="int" resultType="map">
		<![CDATA[
			select cmt_no cmtNo
			        ,post_no postNo
			        ,u.user_no userNo
			        ,name
			        ,content
			        ,to_char(reg_date, 'yyyy-mm-dd hh24:mi') regDate
			        ,group_no
			from comments c, users u
			where c.user_no = u.user_no
			and post_no = #{postNo}
			order by group_no, reg_date
		]]>
	</select>

	<!-- 댓글 등록 -->
	<insert id="insertCmt" parameterType="cmtVo">
		<selectKey keyProperty="cmtNo" resultType="int" order="BEFORE">
			select SEQ_CMT_NO.nextval from dual
		</selectKey>
		<![CDATA[
			insert into comments
			values(#{cmtNo}, #{postNo}, #{userNo}, #{content}, sysdate, #{cmtNo})
		]]>
	</insert>
	
	<!-- 등록한 댓글 가져오기 -->
	<select id="getCmt" parameterType="int" resultType="cmtVo">
		<![CDATA[
			select cmt_no cmtNo
					,post_no postNo
					,user_no userNo
					,content
					,reg_date regDate
					,group_no groupNo
			from comments
			where cmt_no = #{cmtNo}
		]]>
	</select>
	
	<!-- 댓글 수정 -->
	<insert id="updateCmt" parameterType="cmtVo">
		<![CDATA[
			update comments
			set content = #{content}
			where cmt_no = #{cmtNo}
		]]>
	</insert>
	
	<!-- 댓글 삭제 -->
	<delete id="deleteCmt" parameterType="int">
		<![CDATA[
			delete from comments
			where group_no = #{cmtNo}
		]]>
	</delete>

</mapper>